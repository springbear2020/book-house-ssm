<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.whut.bear.panda.dao.UploadMapper">
    <!-- int saveUpload(Upload upload); -->
    <insert id="saveUpload">
        insert into t_upload(user_id, user_type, username, type, status, upload_time, domain, `key`, bucket)
        VALUES (#{userId}, #{userType}, #{username}, #{type}, #{status}, #{uploadTime}, #{domain}, #{key}, #{bucket})
    </insert>

    <!-- List<Upload> getUploadRecord(@Param("type") Integer type, @Param("status") Integer status, @Param("position") Integer position, @Param("offset") Integer offset); -->
    <select id="getUploadRecord" resultType="Upload">
        select *
        from t_upload
        where type = #{type}
          and status = #{status}
        limit #{position},#{offset}
    </select>

    <!-- int updateUploadStatusById(@Param("id") Integer id, @Param("status") Integer status); -->
    <update id="updateUploadStatusById">
        update t_upload
        set status = #{status}
        where id = #{id}
    </update>

    <!-- Upload getUploadById(@Param("id") Integer id); -->
    <select id="getUploadById" resultType="Upload">
        select *
        from t_upload
        where id = #{id}
    </select>
</mapper>